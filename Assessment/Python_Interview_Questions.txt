list comprehension?
dictionary comprehension?
list vs tuple (memory management)
why generator
why decorator
generator vs iterator
differentiate break, continue and pass

what is __init__ ?
why __main__ ?
What is a class and instance?
    A class is a blueprint that is needed to make an object(= instance). The difference between an object and an instance is, an object is a thing and an instance is a relation. In other words, instance describes the relation of an object to the class that the object was made from.

regular/instance method vs class method vs static method?
instance variable vs class variable?

What is alternative constructor using class method?
    create class method using decorator(it should return class object after class method is executed) and call it while instantiating python class

input=pasupathi, output=pasupathi
input= [1,2,3,1,4,5,2,7,4,1,5,8,9] output; dictionary containing repeated element and value as howm many times repeated
regular expession matching NIC interface status, mac, ip etc

